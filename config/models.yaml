# config/models.yaml
global_options:
  temperature: 0.1
  top_p: 0.9
  num_ctx: 8192

# Session settings (lightweight — no RAG)
session:
  chat_history_limit: 5
  history_truncate_chars: 200

models:
  main: "qwen3:8b"
  planner: "qwen3:8b"
  executor: "qwen3:8b"
  critic: "qwen3:8b"
  researcher: "qwen3:8b"
  extraction: "qwen3:8b"

tools:
  enabled:
    - web_pulse_scraper
    - master_file_manager
    - http_client
    - code_executor